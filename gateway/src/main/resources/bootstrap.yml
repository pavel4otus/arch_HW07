spring:
  main:
    banner-mode: "off"
  cloud:
    config:
      uri: http://config:7777
      username: user
      password: passw0rd
  application:
    name: gateway
  profiles:
    active: dev

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
    reactor.netty.http.client: DEBUG
