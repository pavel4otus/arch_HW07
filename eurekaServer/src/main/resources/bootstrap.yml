spring:
  main:
    banner-mode: "off"
  cloud:
    config:
      uri: http://config:7777
      username: user
      password: passw0rd
  application:
    name: eurekaserver
  profiles:
    active: dev
